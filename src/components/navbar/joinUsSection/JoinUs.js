import React from 'react';
import { ButtonContainer } from './JoinUsElements';
import { TransparentButton } from '../../../ButtonElement';

const JoinUs = () => {
  return (
    <ButtonContainer>
      <TransparentButton>Join Us Now</TransparentButton>
    </ButtonContainer>
  );
};

export default JoinUs;
